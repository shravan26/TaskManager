pipeline {
    agent {
        docker {
            label 'docker'
            image "node:${readfile .nvmrc}",
            args '-p 3000:3000'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}
