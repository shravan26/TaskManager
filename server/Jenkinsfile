pipeline {
    agent {
        docker {
            label 'docker'
            image "node:lts-bulleye-slim",
            args '-p 3000:3000'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}
